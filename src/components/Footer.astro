---
import BrandMark from './BrandMark.astro';
import { getLangFromUrl, useTranslations, localizedPath } from '../i18n/utils';

const year = new Date().getFullYear();

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const sitemapLinks = [
  { href: localizedPath('/galerien', lang), label: t('nav.galerien') },
  { href: localizedPath('/kontakt', lang), label: t('nav.kontakt') },
];

const followLinks = [
  { href: 'https://instagram.com/iamguer', label: t('footer.instagram'), external: true },
];

const legalLinks = [
  { href: localizedPath('/impressum', lang), label: t('footer.impressum') },
  { href: localizedPath('/datenschutz', lang), label: t('footer.datenschutz') },
];
---

<footer class="border-t border-line px-4 sm:px-7 pt-7 pb-5 mt-14">
  <div class="grid grid-cols-1 md:grid-cols-3 gap-7 md:gap-10">
    <div>
      <h4 class="font-mono text-[0.68rem] uppercase tracking-[0.24em] text-text-muted mb-3">{t('footer.sitemap')}</h4>
      <ul class="space-y-2 leading-snug">
        {
          sitemapLinks.map((link) => (
            <li>
              <a
                href={link.href}
                class="text-sm text-text-body hover:text-accent transition-colors"
              >
                {link.label}
              </a>
            </li>
          ))
        }
      </ul>
    </div>

    <div>
      <h4 class="font-mono text-[0.68rem] uppercase tracking-[0.24em] text-text-muted mb-3">{t('footer.follow')}</h4>
      <ul class="space-y-2 leading-snug">
        {
          followLinks.map((link) =>
            link.external ? (
              <li>
                <a
                  href={link.href}
                  target="_blank"
                  rel="noopener"
                  class="text-sm text-text-body hover:text-accent transition-colors"
                >
                  {link.label}
                </a>
              </li>
            ) : (
              <li>
                <a
                  href={link.href}
                  class="text-sm text-text-body hover:text-accent transition-colors"
                >
                  {link.label}
                </a>
              </li>
            ),
          )
        }
      </ul>
    </div>

    <div>
      <h4 class="font-mono text-[0.68rem] uppercase tracking-[0.24em] text-text-muted mb-3">{t('footer.legal')}</h4>
      <ul class="space-y-2 leading-snug">
        {
          legalLinks.map((link) => (
            <li>
              <a
                href={link.href}
                class="text-sm text-text-body hover:text-accent transition-colors"
              >
                {link.label}
              </a>
            </li>
          ))
        }
      </ul>
    </div>
  </div>

  <div class="border-t border-line mt-7 pt-3 flex font-mono text-[0.68rem] tracking-[0.14em] text-text-trace">
    <span class="inline-flex items-center gap-2">© {year} <BrandMark size={20} wordmark /></span>
  </div>
</footer>
